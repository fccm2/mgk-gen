This interface to the `magick-core` tries to be able to compile properly,  

If you're using a compatible linux-distribution w/ an installation sys-  
tem than `apt`, read the file `ext/dep.txt`,  

After you install'd the dependencies, you can try to compile w/ the com-  
mand:
```
  $ cd src/
  $ make
```

