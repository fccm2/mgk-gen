## un-tested,

The `"META"` file is `un-tested`.  
The `"find_install"` target from "src/Makefile" is also `un-tested`.
 
## list of test's that do'not pass (0.01.2):

- test-02: abort
- test-07: abort
- test-08: abort
- test-09: abort
- test-12: abort

## test's that was'nt pass'd w/ `0.01.2`:

- fixed in `0.01.3`: forgot a param in draw n' write,

